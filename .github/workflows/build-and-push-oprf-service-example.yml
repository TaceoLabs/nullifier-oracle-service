name: Build and Push (oprf-service-example)

on:
  workflow_dispatch:
  push:
    branches:
      - main
    tags:
      - 'oprf-service-example-v*'

jobs:
  workflow:
    uses: ./.github/workflows/build-and-push.yml
    with:
      image-name: oprf-service-example
      dockerfile: build/Dockerfile.oprf-service-example

